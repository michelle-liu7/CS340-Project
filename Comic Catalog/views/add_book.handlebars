<a href="/">Home</a>
<h1 class="title">Add Books</h1>
<div class="add_new_item">
    <form id="add_book" action="/insertQueries/add_book" method="post">
        <label>Title: <input type="text" name="title" required></label>
        <br>
        <br>
        <label>Issue #: <input type="text" name="issue" required></label>
        <br>
        <br>
        <label>UPC: <input type="text" name="upc" required></label>
        <br>
        <br>
        <label>Price: <input type="text" name="price" required></label>
        <br>
        <br>
        <label>
            Owner:
            <Select name="owner" required>
                {{#each owners}}
                <!--need a getOwners function in insertQueries.js to get a list of owner ids and full names-->
                <option value="{{id}}">{{fullName}}</option>
                {{/each}}
            </Select>
        </label>
        <br>
        <br>
        <label>
            Publisher:
            <Select name="publisher" required>
                {{#each publishers}}
                <!--need a getPublishers function in insertQueries.js to get a list of publisher ids and names-->
                <option value="{{id}}">{{name}}</option>
                {{/each}}
            </Select>
        </label>
        <br>
        <br>
        <label>
            Authors:
            <Select name="authors" multiple size="4" required>
                {{#each authors}}
                <!--need a getAuthors function in insertQueries.js-->
                <option value="{{id}}">{{fullName}}</option>
                {{/each}}
            </Select>
        </label>
        <br>
        <br>
        <label>
            Genres:
            <Select name="genres" multiple size="4" required>
                {{#each genres}}
                <!--need a getGenres function in insertQueries.js-->
                <option value="{{id}}">{{type}}</option>
                {{/each}}
            </Select>
        </label>
        <br>
        <br>
        <!--input type="submit" value="Add" /-->
    </form>
    <button onclick="addBook()">Add</button>
    <br>
    <br>
    <button onclick="window.history.back()">Cancel</button>
</div>
